@page "/logout"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Force a full reload by navigating to the dedicated logout endpoint
        Navigation.NavigateTo("/api/logout", forceLoad: true);
    }
}